import CircleProgress from "./CircleProgress";

const Fallback = () => {
  return (
    <div className="fallback w-full h-full flex justify-center items-center">
      <CircleProgress />
    </div>
  );
};

export default Fallback;
